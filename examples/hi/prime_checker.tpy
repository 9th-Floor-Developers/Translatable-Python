बनाओ प्राइम_चेकर(n: गिनती_संख्या):
    अगर n <= 1:
        वापस_दो गलत

    हर_एक_के_लिए i में हद(2, n):
        अगर n % i == 0:
            वापस_दो गलत
    वापस_दो सही

संख्या: गिनती_संख्या = गिनती_संख्या(लो('संख्या डालो: '))
दिखाओ(प्राइम_चेकर(संख्या))
